// import { PrismaClient } from "@prisma/client";

// const prisma = new PrismaClient();

// export const createSeedTransactionInDB = async (data: any) => {
//   return await prisma.seedTransaction.create({ data });
// };

// export const getSeedTransactionByIdFromDB = async (id: number) => {
//   return await prisma.seedTransaction.findUnique({ where: { id } });
// };

// export const getAllSeedTransactionsFromDB = async () => {
//   return await prisma.seedTransaction.findMany();
// };
